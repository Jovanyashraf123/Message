* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  min-height: 100vh;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #f8eaff;
  font-family: Arial, sans-serif;
  color: #8e3045;
}

.page {
  width: 100%;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  position: relative;
  z-index: 2;
}

h1 {
  font-size: 28px;
  margin-bottom: 7px;
}

.subtitle {
  font-size: 14px;
  color: #a65b6d;
  margin-bottom: 20px;
}

.envelope-area {
  width: 340px;
  height: 270px;
  display: flex;
  justify-content: center;
  align-items: flex-end;
  perspective: 900px;
}

.envelope {
  position: relative;
  width: 320px;
  height: 205px;
  filter: drop-shadow(0 10px 15px rgba(90, 30, 50, .18));
}

.back {
  position: absolute;
  inset: 0;
  background: #df4559;
  border-radius: 5px;
  z-index: 1;
}

.letter {
  position: absolute;
  z-index: 2;
  width: 276px;
  min-height: 190px;
  left: 22px;
  top: 14px;
  padding: 17px 15px;
  background: #fffdfd;
  text-align: center;
  box-shadow: 0 2px 10px rgba(0,0,0,.09);
  transition: top .85s cubic-bezier(.2,.8,.2,1);
}

.to {
  display: block;
  text-align: left;
  font-family: cursive;
  font-size: 13px;
  margin-bottom: 15px;
}

.letter p {
  font-family: Georgia, serif;
  font-style: italic;
  font-size: 18px;
  line-height: 1.35;
  margin: 2px 0;
}

.signature {
  margin-top: 10px;
  font-family: cursive;
  font-size: 15px;
}

.flap {
  position: absolute;
  z-index: 5;
  left: 0;
  top: 0;
  width: 0;
  height: 0;
  border-left: 160px solid transparent;
  border-right: 160px solid transparent;
  border-top: 105px solid #d63f53;
  transform-origin: top center;
  transition: transform .85s ease;
}

.left-fold {
  position: absolute;
  z-index: 6;
  left: 0;
  bottom: 0;
  width: 0;
  height: 0;
  border-top: 103px solid transparent;
  border-bottom: 103px solid transparent;
  border-left: 160px solid #ef8d99;
}

.right-fold {
  position: absolute;
  z-index: 6;
  right: 0;
  bottom: 0;
  width: 0;
  height: 0;
  border-top: 103px solid transparent;
  border-bottom: 103px solid transparent;
  border-right: 160px solid #ef8d99;
}

.bottom-fold {
  position: absolute;
  z-index: 7;
  left: 0;
  bottom: 0;
  width: 0;
  height: 0;
  border-left: 160px solid transparent;
  border-right: 160px solid transparent;
  border-bottom: 103px solid #ff626b;
}

.seal {
  position: absolute;
  z-index: 8;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 31px;
  height: 31px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  background: #d91f51;
  color: white;
  font-size: 17px;
  box-shadow: 0 3px 8px rgba(0,0,0,.18);
  transition: opacity .25s ease, transform .25s ease;
}

.envelope.open .flap {
  transform: rotateX(180deg);
  z-index: 0;
}

.envelope.open .letter {
  top: -145px;
  z-index: 4;
}

.envelope.open .seal {
  opacity: 0;
  transform: translate(-50%, -50%) scale(.4);
}

.buttons {
  display: flex;
  gap: 16px;
  margin-top: 35px;
}

button {
  width: 140px;
  height: 46px;
  border: 2px solid #c74659;
  border-radius: 6px;
  background: transparent;
  color: #b63b4e;
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  transition: .2s ease;
}

button:hover,
button.active {
  background: #c74659;
  color: white;
  box-shadow: 0 5px 12px rgba(199,70,89,.22);
}

button:active {
  transform: scale(.95);
}

/* Floating hearts */
.hearts {
  position: fixed;
  inset: 0;
  pointer-events: none;
  overflow: hidden;
}

.heart {
  position: absolute;
  bottom: -30px;
  animation: floatUp linear forwards;
  opacity: 0;
  font-size: 18px;
}

@keyframes floatUp {
  0% {
    transform: translateY(0) scale(.7) rotate(0deg);
    opacity: 0;
  }
  15% { opacity: .75; }
  100% {
    transform: translateY(-110vh) scale(1.2) rotate(25deg);
    opacity: 0;
  }
}

@media (max-width: 500px) {
  h1 { font-size: 24px; }
  .envelope-area { transform: scale(.86); margin-top: -5px; }
  .buttons { margin-top: 15px; }
}